# import scratchattach as sa
# from scratchattach import Encoding
# scratch_username = "username"
# scratch_password = "password"
# cloud = session.connect_cloud("1149116249")
# cloud.set_var("Title", 'Encoding.encode("Welcome!")')
# cloud.set_var("Contents", 'Encoding.encode("This project is a place where scratch news is shared. Stay tuned for more info!")')
# cloud = session.connect_tw_cloud("1149116249")
# cloud.set_var("Title", 'Encoding.encode("Welcome!")')
# cloud.set_var("Contents", 'Encoding.encode("This project is a place where scratch news is shared. Stay tuned for more info!")')
